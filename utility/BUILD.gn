# Group depending on upstream's counterpart to make it easier to manage
# dependencies in vortex/ (//chrome/utility does not depend on //vortex/utility,
# so //vortex/utility becomes essentially an alias for //chrome/utility now).
group("utility") {
  public_deps = [ "//chrome/utility" ]
}
